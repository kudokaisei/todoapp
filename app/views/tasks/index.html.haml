.Header
  .HeaderTitle
    %h2 ToDo App
  .HeaderEdit
    %h2 
      ユーザー名:
      = current_user.name
      = link_to edit_user_path(current_user) do
        = icon('fas', 'cog', class: 'icon')
.Main
  .Mainbox
    .Name
      %h3 仕事🖥
    .Form
      %p タスク追加
      = link_to '➕', new_job_path, class:"btn"
    .Taskzone
      - @jobs.each do |job|
        .Task
          .Checkbox
            %input.Box{type: "checkbox"}
          .Contentbox
            .Content
              = link_to job_path(job.id), class: "task" do
                = job.task
            .Sub
              = link_to '削除🗑', job_path(job.id), method: :delete, class:"delete"
              = link_to '編集✒️', edit_job_path(job.id), class:"edit"
              

  .Mainbox
    .Name
      %h3 家族👨‍👩‍👧
    .Form
      %p タスク追加
      = link_to '➕', new_family_path, class:"btn"
    .Taskzone
      - @families.each do |family|
        .Task
          .Checkbox
            %input.Box{type: "checkbox"}
          .Contentbox
            .Content
              = link_to family_path(family.id), class: "task" do
                = family.task
            .Sub
              = link_to '削除🗑', family_path(family.id), method: :delete, class:"delete"
              = link_to '編集✒️', edit_family_path(family.id), class:"edit"

  .Mainbox
    .Name
      %h3 日課🏃‍♂️
    .Form
      %p タスク追加
      = link_to '➕', new_routine_path, class:"btn"
    .Taskzone
      - @routines.each do |routine|
        .Task
          .Checkbox
            %input.Box{type: "checkbox"}
          .Contentbox
            .Content
              = link_to routine_path(routine.id), class: "task" do
                = routine.task
            .Sub
              = link_to '削除🗑', routine_path(routine.id), method: :delete, class:"delete"
              = link_to '編集✒️', edit_routine_path(routine.id), class:"edit"

  .Mainbox
    .Name
      %h3 その他
    .Form
      %p タスク追加
      = link_to '➕', new_other_path, class:"btn"
    .Taskzone
      - @others.each do |other|
        .Task
          .Checkbox
            %input.Box{type: "checkbox"}
          .Contentbox
            .Content
              = link_to other_path(other.id), class: "task" do
                = other.task
            .Sub
              = link_to '削除🗑', other_path(other.id), method: :delete, class:"delete"
              = link_to '編集✒️', edit_other_path(other.id), class:"edit"