.Task-edit
  .task
    %p タスク作成
    = form_with(model: @job, local: true, class: "form") do |form|
      = form.text_field :task, placeholder: "タスク内容", class: 'content'
      = form.text_area :details, placeholder: "タスクの詳細（期日、担当者など）", class: 'area'
      = form.submit "追加", class: 'button'


